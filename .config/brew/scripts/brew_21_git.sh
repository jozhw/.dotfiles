#!/usr/bin/env bash
#
# Git — version-control tooling. (git itself is installed by install.sh.)
#
# Add here: git extensions and helpers (git-lfs, git-delta, lazygit, gh, ...)
# and any other version-control systems.

#brew install git       # Distributed version control system (installed elsewhere)
brew install git-lfs    # Git Large File Storage — version large binaries in Git
