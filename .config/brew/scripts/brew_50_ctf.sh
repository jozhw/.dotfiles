#!/usr/bin/env bash
#
# CTF & security — capture-the-flag, pentest, forensics, and reverse-engineering.
# See https://github.com/ctfs/write-ups.
#
# Add here: crackers, network/wireless auditing, binary/firmware analysis,
# forensics/recovery, and crypto-attack tools.

brew install aircrack-ng    # Wi-Fi network security auditing suite
brew install bfg            # Quickly strip large files/secrets from Git history
brew install binutils       # GNU binary utilities (objdump, readelf, etc.)
brew install binwalk        # Analyze and extract firmware images
brew install cifer          # Automatic classical-cipher cracking tool
brew install dex2jar        # Convert Android .dex to Java .class files
brew install dns2tcp        # Tunnel TCP connections over DNS
brew install fcrackzip      # Brute-force/dictionary ZIP password cracker
brew install foremost       # Carve/recover files from disk images by header
brew install hashpump       # Perform hash length-extension attacks
brew install hydra          # Parallelized network login (password) cracker
brew install john           # John the Ripper password hash cracker
brew install knock          # Port-knocking client and server
brew install netpbm         # Toolkit for manipulating graphic images
brew install nmap           # Network discovery and port/security scanner
brew install pngcheck       # Verify integrity of PNG files
brew install socat          # Multipurpose socket relay (SOcket CAT)
brew install sqlmap         # Automatic SQL-injection and DB takeover tool
brew install tcpflow        # Reconstruct and record TCP session data
brew install tcpreplay      # Replay captured network traffic
brew install tcptrace       # Analyze tcpdump capture files
brew install ucspi-tcp      # Command-line tools for building TCP client/server apps
#brew install xpdf          # PDF viewer/tools (disabled — conflicts with poppler)
brew install xz             # XZ/LZMA data compression utility
