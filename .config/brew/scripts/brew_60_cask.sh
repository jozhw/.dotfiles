#!/usr/bin/env bash
#
# Casks — GUI (desktop) applications, installed via `brew install --cask`.
#
# Add here: anything that is a Homebrew *cask* — GUI apps, fonts, and other
# non-CLI artifacts. Command-line formulae belong in a category script above.

brew install --cask proton-pass    # Proton Pass password manager
brew install --cask proton-mail    # Proton Mail desktop client
brew install --cask emacs          # Emacs text editor (GUI build)
brew install --cask zen            # Zen web browser
brew install --cask arc            # Arc web browser
brew install --cask positron       # Positron IDE — data-science IDE, RStudio successor
brew install --cask macfuse        # FUSE for macOS — mount user-space filesystems
brew install --cask tor-browser    # Tor Browser for anonymous browsing
brew install --cask qbittorrent    # qBittorrent BitTorrent client
brew install --cask vlc            # VLC media player
brew install --cask docker         # Docker Desktop container runtime
brew install --cask ghostty        # Ghostty — native GPU-accelerated terminal emulator
brew install --cask skim           # Skim PDF reader and annotator
brew install --cask zotero         # research paper organizater and citer
brew install --cask rectangle      # window manager
