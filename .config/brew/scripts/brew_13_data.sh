#!/usr/bin/env bash
#
# Data — utilities for wrangling structured data and shell pipelines.
#
# Add here: pipeline monitors, and JSON/CSV/columnar processors (jq, miller,
# csvkit, xsv, ...). Database clients also fit here.

brew install pv    # Pipe viewer — show progress/throughput of data through a pipe
