---
title: Immich
description: Self-hosted photo library.
---

Immich runs as `root`, so make sure to give it the proper access. In principle, I like to separate application `data` and the asset `library`. However, in the case for Immich, they are all one directory.
